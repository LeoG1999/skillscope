# 对照条件

两个条件，共用同一套后端：同一个执行脊柱、同一个模型、同样的数据源与执行预算。
唯一的差别是交互层。这样比较分离出来的就是交互设计本身的贡献，而不是模型或工具能力的差异。

参照 Cocoa（CHI '26）的做法：基线是同一个 agent、同一个文档编辑器，只把交互换成聊天框。

## 条件 A — SkillScope（当前系统）

结构化工作区。数据源、工作流、决策依据、影响力探测、判据构建、三档范围、
相邻情况追问、封存判断、配对对比、四出口与评审记录，均以独立面板呈现，
状态在面板内变化。

## 条件 B — 纯聊天（待建）

同一后端，界面只有一个对话框。所有能力仍然存在，但只能通过对话调用，
结果以文字返回，没有面板、没有可点的指令、没有并排对比。

具体要求：

- 共用 `server.py` 的全部端点，不新增能力，也不削减能力
- 智能体可以主动调用探测与对比，但结果以自然语言叙述，不渲染结构
- 数据源可读可改，但通过对话进行，不提供日历或表格界面
- 保留完整日志：每一次输入、每一次端点调用、每一个返回
- 以配置档切换，不做成两套代码

## 为什么是这两个

系统论文要证明的是交互模型的价值，不是"有证据比没证据好"。
把能力固定、只换交互，才回答得了"这套工作区是否真的帮助了判断"。

## 待定

- 场次时长与被试数
- 任务域（差旅规划 / 报销审核，两个预设都已带场景数据）
- 条件内的任务计数平衡
- 纯聊天条件下如何采集预先声明的预期（若无面板，需用表单或对话中显式记录，
  否则范围对齐这一指标在该条件下不存在，因果解释会断）
